apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tempogate.fullname" . }}-test-connection"
  labels:
    {{- include "tempogate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tempogate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
