# Screenshots — getting-started.md

Drop the three PNGs below in this directory; `../../getting-started.md`
already references them by these exact names. All are from the local
docker-compose demo, so there is nothing sensitive to redact.

| File | Should show |
| --- | --- |
| `01-ui-signin.png` | `http://temporal-ui:8080` before login — the Temporal UI **Sign in** screen. |
| `02-consent.png` | The bundled mock IdP consent page: "Continue as **alice@example.com**?" with the **Approve** link. |
| `03-authenticated.png` | Back in the Temporal UI, authenticated — the namespaces list rendered. |

Recommended: ~1400px wide, capture the whole browser viewport so the URL bar
(showing the `tempogate` / `temporal-ui` hostnames) is visible.
